[pytest]
testpaths = src/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    ingestion: Tests for Bronze layer ingestion
    transformation: Tests for Silver layer transformation
    business: Tests for Gold layer business logic
    notifications: Tests for Slack notifications
    integration: Integration tests requiring Docker
